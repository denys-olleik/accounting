@* @model Accounting.Models.SecretViewModels.DeleteSecretViewModel

@{
  ViewData["Title"] = "Delete Secret - " + ConfigurationSingleton.Instance.ApplicationName;
}

@{
  string headerText = "Delete Secret";
}
<partial name="_H1HeaderPartial" model="@headerText" />

<form asp-action="Delete" asp-asp-controller="Secret" method="post"
      class="display-flex flex-direction-column margin-bottom-5px">

  <input asp-for="SecretID" type="hidden" />

  <div class="display-flex">
    <div class="display-flex flex-1 flex-direction-column background-color-antiquewhite margin-bottom-5px box-shadow">
      <label asp-for="Key">Key</label>
      <input asp-for="Key" class="font-size-20px" readonly />
    </div>
    <div class="width-5px"></div>
    <div class="display-flex flex-1 flex-direction-column background-color-antiquewhite margin-bottom-5px box-shadow">
      <label asp-for="Type">Type</label>
      <input asp-for="Type" class="font-size-20px" readonly />
    </div>
  </div>

  <div class="display-flex flex-direction-column margin-bottom-5px background-color-antiquewhite box-shadow">
    <label asp-for="Purpose">Purpose</label>
    <input asp-for="Purpose" class="font-size-20px" readonly />
  </div>

  <div class="display-flex">
    <div class="flex-1">
      <button class="full-width-regular-button width-100" type="button" onclick="event.preventDefault(); window.location.href='@Url.Action("Secrets", "Secret")';">Cancel</button>
    </div>
    <div class="width-5px"></div>
    <div class="flex-1">
      <button type="submit" class="full-width-orange-red-button width-100">Delete</button>
    </div>
  </div>
</form> *@

@*   public class DeleteTenantViewModel
  {
    public int TenantId { get; set; }
    public bool DeleteDatabase { get; set; }
  } *@

@model Accounting.Models.Tenant.DeleteTenantViewModel

@{
  ViewData["Title"] = "Delete Tenant - " + ConfigurationSingleton.Instance.ApplicationName;
}

@{
  string headerText = "Delete Tenant";
}

<partial name="_H1HeaderPartial" model="@headerText" />

<form asp-action="Delete" asp-asp-controller="Tenant" method="post"
      class="display-flex flex-direction-column margin-bottom-5px">

  <input asp-for="TenantId" type="hidden" />

  <div class="display-flex flex-1 flex-direction-column background-color-antiquewhite margin-bottom-5px">
    <span>
      <input asp-for="DeleteDatabase" class="font-size-20px" readonly />
      <label asp-for="DeleteDatabase">Delete Database</label>
    </span>
  </div>

  <div class="display-flex">
    <div class="flex-1">
      <button class="full-width-regular-button width-100" type="button" onclick="event.preventDefault(); window.location.href='@Url.Action("Tenants", "Tenant")';">Cancel</button>
    </div>
    <div class="width-5px"></div>
    <div class="flex-1">
      <button type="submit" class="full-width-orange-red-button width-100">Delete</button>
    </div>
  </div>
</form>