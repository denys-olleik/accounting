@model Accounting.Models.UserViewModels.CreateUserViewModel

@{
  ViewData["Title"] = "Create user - " + ConfigurationSingleton.Instance.ApplicationName;
}

@{
  string headerText = "Create user";
}
<partial name="_H1HeaderPartial" model="@headerText" />

<div class="display-flex margin-bottom-5px background-color-antiquewhite">
  <div class="margin-left-20px flex-1 text-align-right">
    <a asp-action="Users" asp-controller="User" class="font-Roboto font-size-16px color-blue">Users</a>
    <a asp-action="Index" asp-controller="Home" class="font-Roboto font-size-16px color-blue margin-left-20px">Home</a>
  </div>
</div>

<form asp-action="Create" asp-controller="User" method="post" class="display-flex flex-direction-column">
  <div class="display-flex flex-direction-column">
    <label asp-for="Email" class="background-color-antiquewhite"></label>
    <input asp-for="Email" class="margin-bottom-5px font-size-20px" />
  </div>
  <div class="display-flex max-width-768-flex-direction-column">
    <div class="flex-1 display-flex flex-direction-column">
      <label asp-for="FirstName" class="background-color-antiquewhite">First name</label>
      <input asp-for="FirstName" class="margin-bottom-5px font-size-20px" />
    </div>
    <div class="width-5px"></div>
    <div class="flex-1 display-flex flex-direction-column">
      <label asp-for="LastName" class="background-color-antiquewhite">Last name</label>
      <input asp-for="LastName" class="margin-bottom-5px font-size-20px" />
    </div>
  </div>
  <div class="display-flex flex-direction-column background-color-antiquewhite margin-bottom-5px">
    <div class="display-flex align-items-center">
      <input asp-for="SendInvitationEmail" type="checkbox" id="send-invitation-email" />
      <label for="send-invitation-email" class="margin-left-5px">Send invitation email</label>
    </div>
  </div>
  <button type="submit" class="full-width-regular-button">Create user</button>
</form>

@if (Model != null && Model.ValidationResult != null)
{
  foreach (var error in Model.ValidationResult.Errors)
  {
    <div>
      <small class="color-red">@error.ErrorMessage</small>
    </div>
  }
}